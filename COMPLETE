LayerFlow `lf` CLI — implementation status

Blueprint coverage (docs/lf-terminal.md): all sections have package structure and
constructor wiring in place.

Verified this session: the module now builds, vets, and tests cleanly
(`go build ./...`, `go vet ./...`, `go test -race ./...` all pass on Go 1.26),
and `bin/lf` runs. This required fixing code that had never compiled:
stale `muesli/ansi` dependency (glamour bumped to v1.0.0, `go.sum` generated),
mixed packages in `cmd/lf`, a misplaced `views/` package, duplicate `DiffView`
structs, a command-table init cycle, and assorted API-drift errors.

Still stubs (honest): `chat`, `run`, `sync`, `rescue`, `upgrade`, and
`mcp add/remove/health`. The TUI, tools, and providers are wired but not yet
driving real provider calls.
